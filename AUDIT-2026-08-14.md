# AUDIT-2026-08-14 — portfolio (eivinasn.com)

Close-out audit against `repo-audit-closeout-prompt.md`. Repo 1 of 3.

**Repo:** `github.com/heshipstech/2026-portfolio` · **PUBLIC** · default branch `main`
**Live:** https://eivinasn.com/ · static Astro on LiteSpeed/Hostinger
**Started:** 2026-08-14 from tip `b82977e`

165 checks were run by nine parallel investigators, each required to cite the
command it ran; every non-passing finding then went to an adversarial verifier
that tried to refute it. Three findings were refuted or corrected that way and
are recorded as such below.

Legend: `PASS` / `FAIL` / `BLOCKED` / `PARK`. STOP-list items are in
[NEEDS-REVIEW.md](NEEDS-REVIEW.md) and were not acted on.

Standing constraint from [CLAUDE.md](CLAUDE.md) §1: **content, design,
photography, icons and copy are founder-approved and frozen.** Findings that
would require changing them are reported, never edited.

---

## Phase 1 — Git and working tree state

- [x] `git status` clean — **PASS.** Empty at `b82977e`.
- [x] Untracked files triaged — **PASS.** None in limbo.
- [x] No interrupted operations — **PASS.** No `MERGE_HEAD`, `REBASE_HEAD`, `CHERRY_PICK_HEAD`, `BISECT_LOG`.
- [x] Not in detached HEAD — **PASS.** On `main`.
- [x] `git stash list` empty — **PASS.**
- [x] Unpushed commits — **PASS.** `git log --branches --not --remotes` empty.
- [x] Ahead/behind resolved — **PASS.**
- [x] Feature branches merged or explained — **PASS.** Only `main` exists, locally and remote.
- [x] Stale merged branches deleted — **PASS.** None.
- [x] Remote-tracking refs pruned — **PASS.** `git remote prune origin --dry-run` reports nothing.
- [x] Worktrees — **PASS.** One, the main checkout.
- [x] Submodules — **PASS (N/A).** No `.gitmodules`.
- [x] Tags — **PASS (N/A).** None local or remote; `private: true`, never published, no release cut.
- [x] `.gitignore` correct; nothing tracked that it ignores — **PASS**, with one fix applied: `.env.*` was also swallowing `.env.example`, which three tracked files told readers to consult. Negated and tracked in `c3026c4`.
- [x] No files over ~5 MB — **PASS.** Largest is `src/assets/hero-portrait.png`, 512 KB.
- [x] Commit author identity — **PARK (STOP #3).** 25 of 40 commits carry the personal Gmail address in a public history. Current local identity is the noreply address. Changing the past requires a rewrite. The verifier corrected the original finding here: it claimed the address was otherwise unpublished, but Q12 rules it published on every page deliberately, so the exposure is the _metadata_, not the address.
- [x] Default branch is the one developed against — **PASS.** `main`.

## Phase 2 — Secrets, credentials, and exposed data

The priority phase. Run with gitleaks 8.30.1 plus independent manual sweeps.

- [x] Working-tree secret scan — **PASS.** `gitleaks detect --source . --no-git --redact`: no leaks, 1.33 MB scanned. Caveat caught and closed: `--no-git` honours `.gitignore`, so `dist/`, `.astro/`, `.env.example` and the planning doc were skipped; they were copied to a scratch directory and rescanned separately. Also clean.
- [x] Manual grep sweep, provider patterns — **PASS.** Zero matches across all 97 tracked files for private-key headers, `AKIA`, `ghp_`, `xoxb-`, `AIza`, JWTs, or database URLs.
- [x] Manual grep sweep, generic keywords — **PASS.** 41 hits, all reviewed: prose, comments, Permissions-Policy directives, and `${{ secrets.X }}` interpolations. No literal values.
- [x] No `.env*` tracked, now or ever — **PASS.**
- [x] No key material committed — **PASS.** No `.pem`, `.key`, `.p12`, `id_rsa`, service-account JSON, `.npmrc`, `.pypirc` or `.netrc`, in the tree or in history.
- [x] No hardcoded credentials in source/config/fixtures — **PASS.** There are no test, fixture, seed or migration files at all; the single ≥32-char literal in tracked source is a 404 probe path.
- [x] **Full-history secret scan — FAIL, CRITICAL, and already exposed.** See [NEEDS-REVIEW.md](NEEDS-REVIEW.md) §1. The 2026-08-13 `filter-repo` purge did not remove anything from GitHub: pre-purge objects are still served to an anonymous `git fetch <sha>`, GitHub's own public events feed publishes every pre-rewrite SHA, and the repo has been public since 2026-01-11 rather than since the purge. The Hostinger SSH username, the server IP and the purged planning document are all still retrievable. Verified twice, independently.
- [x] Client-bundle leakage — **PASS.** Six env vars are read, all `PUBLIC_ANALYTICS_*`, all deliberately public, and none is set — the built output contains no analytics script and no variable values.
- [x] Source maps — **PASS.** None emitted into `dist/`.
- [x] No secrets in `.md` files or comments — **PASS.** Every documented credential uses an explicit placeholder (`<your-ssh-host>`, `uXXXXXXXXX`).
- [x] No secrets in CI config — **PASS.** Only `${{ secrets.* }}` references; none echoed.
- [x] No build output or `node_modules` committed — **FAIL, not acted on.** Two `.astro` cache files (`settings.json`, `types.d.ts`, ~150 B of inert content) are in the published history from the initial commit. Removing them means a second rewrite of a public history for no security gain; `.gitignore` prevents recurrence.
- [x] No junk/backup files — **PASS.**
- [x] No dumps, `.sql`, `.sqlite`, CSVs or logs with user data — **PASS.** None exist.
- [x] Internal-only material published — **PARK.** Two findings, both in [NEEDS-REVIEW.md](NEEDS-REVIEW.md) §3 and §4: the case-study photography and NFQ figures already accepted in Q23 (re-verified as still present, plus the OG cards that Q23 predates), and sibling-repo operational detail in QUESTIONS.md. Verified that none of it reaches `dist/`.
- [x] `.env.example` complete, placeholders only — **FIXED** in `c3026c4`. It existed but was gitignored and therefore invisible to everyone but the founder's machine.
- [x] Deployed site: `/.git/`, `/.env`, directory listing, admin routes — **PASS.** Checked live.
- [x] Backend rules / CORS — **PASS (N/A).** No backend, no CORS-bearing endpoint.
- [x] Biometric/participant data — **PASS (N/A)** for this repo.
- [x] Anything found in history → STOP list — **DONE.** Nothing deleted; everything recorded with SHAs.

## Phase 3 — Dependencies and supply chain

- [x] Lockfile committed and in sync — **PASS.** `npm ci` resolves.
- [x] `npm audit` — **PASS.** **0 vulnerabilities at any level.** The 13 advisories Q3 recorded were resolved by the Astro 7 upgrade in `223f308`; Q3 amended in `c3026c4` to record the reversal, and the CI gate tightened from `critical` to `high`.
- [x] `npm outdated` — reviewed. No patch or minor bumps outstanding. Majors: none pending beyond the Astro line already taken.
- [x] Unused / undeclared dependencies — **FIXED.** `sharp` was imported by `scripts/generate-icons.mjs` and declared nowhere, resolving only transitively through `astro`; now declared. `axe-core` is redundant with `@axe-core/playwright`'s own dependency but was **kept deliberately** — it pins the axe rule set explicitly, and a drifting rule set would make a11y results non-reproducible.
- [x] Deprecated/unmaintained packages — **PASS.** None flagged by the install.
- [x] Suspicious install scripts — **PASS**, with a note: npm 11 reports three packages with un-approved install scripts (`esbuild`, `fsevents` ×2). All three are ordinary build-toolchain packages; the warning is npm's new allow-list prompt, not a finding.
- [x] Licences — **PASS.** No GPL/AGPL in the dependency tree.
- [x] Node version pinned consistently — **FIXED.** `engines.node` said `>=20.3.0`, two majors below Astro 7's own floor, so the declared floor could not build the project. Now `>=22.12.0`, matching `.nvmrc` (22) and both workflows.
- [x] Dependabot or Renovate — **FIXED.** Neither existed; added `.github/dependabot.yml` with grouped minor/patch, separate majors, and monthly Actions updates.

## Phase 4 — Build, types, lint, tests

- [x] Clean build — **PASS.** `npm run build` completes with no errors and no new warnings.
- [x] Typecheck — **PASS**, with a real limit: `tsconfig.json` includes `**/*.mjs` but sets `checkJs: false`, so the ten build scripts (1,370 LOC) get grammar checking only. Proven, not assumed: an injected undefined call and a bad `JSON.parse()` in `check-links.mjs` produced 0 errors. Turning `checkJs` on is a follow-up, not a drive-by.
- [x] Lint — **BLOCKED.** No linter exists: no script, no dependency, no config. Recorded in [NEEDS-REVIEW.md](NEEDS-REVIEW.md) §5 as a programme decision.
- [x] Formatter — **PASS.** `prettier --check .` clean across the repo.
- [x] Full test suite — **FAIL (no suite exists).** `npm test` is undefined and there are zero test files. `npm run verify` is eight end-to-end gates over the built output, which is real verification but is not unit testing. Parked as a founder decision.
- [x] No `.only` / `.skip` / commented-out tests — **PASS (N/A).**
- [x] Coverage — **not collected**, and there is nothing to instrument. The verifier corrected the original finding's second half: the claim that every file changed in the last 24h was wrong about the git history. The meaningful metric here is _page_ coverage of the gates, which is the next line.
- [x] Gate page coverage — **FIXED** in `e6703a6`. `/privacy/` was in the sitemap and live but absent from all three browser gates. Now derived from the built sitemap.
- [x] Build output size — **PASS.** Re-measured and CLAUDE.md §4 updated; five of its eight numbers had drifted.

## Phase 5 — Code hygiene

- [x] No `console.log` / `debugger` in shipped code — **PASS.**
- [x] TODO/FIXME/HACK inventory — **PASS.** None outstanding in tracked source.
- [x] Commented-out code — **PASS.**
- [x] Dead code and unused files — **PARTIALLY FIXED.** Dead CSS removed in `b0b0ed3` (`.no-scrollbar`, and the hand-written `.delay-*` that duplicated Tailwind's own utilities). **BLOCKED:** deleting the seven unreferenced files in `public/` (`favicon.svg` + six `logo-*.png`, 5,709 B, all live on the server) was refused by the tooling; recorded in FOUNDER-TASKS.md §6 and CLAUDE.md §7 so it is not lost.
- [x] No hardcoded localhost / personal IPs in production paths — **PASS.** The `localhost` uses are in gate scripts, which is where they belong.
- [x] No placeholder content in shipped output — **PASS.** No `example.com`, no lorem, in `dist/`.
- [x] Feature flags at intended values — **PASS.** The analytics slot is genuinely inert in `dist/` (Q22). `Nav`'s `showCta` defaults false, leaving three guarded CTA blocks unreachable — deliberate and documented in the component.
- [x] No empty `catch {}` — **PASS.**
- [x] Env vars validated at boot — **PASS (N/A).** Static build; nothing reads env at runtime.
- [x] Naming and structure consistent — **PASS.** No `*-copy`, `*-new`, `old/`.

## Phase 6 — Documentation

The worst-drifted area, and the largest part of tonight's work. All fixed in
`c3026c4` and `b0b0ed3`.

- [x] README current — **FIXED.** Advertised Astro 4 and Tailwind 3 to the public; both wrong by three majors and one. Page count corrected.
- [x] README setup instructions work — **PASS.** Commands exist and run.
- [x] Every README command exists in `package.json` — **PASS.**
- [x] CLAUDE.md current — **FIXED, and this was the most consequential.** Four of six stack rows wrong; the `script-src` trap **inverted** — it told the reader no script hashes are needed, which under Astro 7 would strand ~20 homepage blocks at `opacity: 0`. Budgets re-measured. Three new traps added: the repo is public, a force-push deletes nothing from GitHub, and Tailwind 4 scans Markdown.
- [x] CHANGELOG.md — **FIXED.** Did not exist; reconstructed from history and cross-checked against BACKLOG.md.
- [x] LICENSE — **PARK.** Absent, so the repo is all-rights-reserved by omission rather than decision. Founder ruling needed.
- [x] `docs/` — **PASS (N/A).** No such directory.
- [x] API/route docs — **PASS (N/A).** Static site.
- [x] Architecture decisions from the last 24h recorded — **FIXED.** Q3 and Q14 amended; the post-ladder work recorded in BACKLOG in `b82977e`.
- [x] Internal links resolve — **PASS.** Including the `#qNN` anchors.
- [x] External links return 200 — **PASS**, one documented exception: LinkedIn serves HTTP 999 to automation, so the site's only outbound link is unverifiable by machine. Founder to open it once.
- [x] Badges — **PASS (N/A).** None.
- [x] Name/version/description consistent — **FIXED.** `package.json` gained `description`, `repository`, `homepage`.
- [x] Contradicting inline comments — **FIXED.** Four in `global.css`, one in `generate-htaccess.mjs`, two in `ci.yml`.
- [x] CONTRIBUTING.md / SECURITY.md — **FIXED.** Both added; the repo is public and had no disclosure route.

## Phase 7 — Config, CI/CD, deployment

- [x] `package.json` metadata — **FIXED.** `private: true` was already correct.
- [x] Every script runs — **PASS** for the ten that are safe to run. **BLOCKED** for four, honestly: `generate:og`, `generate:icons` and `generate:fonts` write into the working tree, and `ping:indexnow` would tell four search engines to recrawl the live site. Not side effects to trigger during an audit.
- [x] CI green on `main` — **PASS**, verified by SHA rather than by newest row.
- [x] Actions least-privilege and SHA-pinned — **PASS.** Both workflows declare `contents: read`; every third-party action is pinned to a full commit SHA; no `pull_request_target`.
- [x] Supply chain: gitleaks binary — **FIXED.** The install step fetched a release asset and `sudo install`ed it with no verification, justified by a comment claiming tag immutability. A release asset is a separately uploaded blob; a tag pin is not a file pin. Now checksum-verified before extraction.
- [x] Secrets referenced in CI exist — **PASS.** Verified in both directions.
- [x] Dockerfile — **PASS (N/A).**
- [x] Deployment config per environment — **PASS.** No staging environment exists to leak.
- [x] `${{ }}` interpolated into `run:` bodies — **PARK.** Five expansions in `deploy.yml` have the classic script-injection shape. Severity is low because every value is settable only by the repo owner. Not changed: this is the pipeline the site publishes through, it cannot be tested without triggering a real deploy against a host that rate-limits SSH, and the founder is asleep. Fix is mechanical — bind each through `env:` and compare the shell variable.
- [x] `DEPLOY_DELETE=true` contradicts its own documentation — **PARK.** Repository-variable change is a founder action; currently benign.
- [x] Security headers live — **PASS.** All six present, CSP with a real `default-src 'none'`.
- [x] HTTPS enforced, certificate valid — **PASS.**
- [x] Form endpoints — **PASS (N/A).** The site has no form.
- [x] Privacy policy accurate — **PASS.** No analytics, no cookies, nothing collected; the notice says so and now names Lithuania and the supervisory authority.

## Phase 8 — Public surface

- [x] Crawl for 404s and broken links — **PASS.** 49 links checked, 0 broken.
- [x] `robots.txt` and `sitemap.xml` — **PASS.** 6 URLs, all resolve, nothing private exposed.
- [x] Meta, OG/Twitter, favicon, canonical on every page — **PASS**, with one defect **PARKED**: `og:image` is now per-page but `og:image:alt` is still global, so every page describes the homepage card. Fixing it means writing per-page alt copy, which is site copy.
- [x] 404 page exists and is styled — **PASS.**
- [x] Images optimised, sized, alt text — **PASS.** Largest first-load image 13.3 KB.
- [x] Accessibility pass — **PASS.** 0 axe violations across 7 pages at every impact level, plus the checks axe cannot make: skip link, stranded reveals, reduced motion, focus management.
- [x] Lighthouse on main pages — **BLOCKED.** No Lighthouse tooling in this repo; `measure:perf` covers the same ground with real Chromium measurements against enforced budgets, recorded in CLAUDE.md §4. Adding Lighthouse CI is a follow-up, not a gap in what is verified.
- [x] Renders correctly at mobile width — **PASS.** 390px, 4× CPU throttle, slow 4G: homepage 107.7 KB / LCP 644 ms, case study 81.0 KB / LCP 784 ms, CLS 0 on both.
- [x] Code-dependent claims in site copy verified — **PASS.** The site makes no test-count or accuracy claim.

## Phase 9 — Cross-repo consistency

Deferred until all three repos are audited; results recorded here and in the
other two audit files.

- [ ] Node version, package manager, linter, formatter compared
- [ ] Shared brand assets compared
- [ ] `.gitignore` baseline compared
- [ ] Diverged copy-pasted code
- [ ] Visibility stated per repo

## Phase 10 — Close out

- [x] All work committed and pushed — see the commit list below.
- [x] All intended merges done — **N/A**, no branches.
- [x] `git status` clean, nothing unpushed.
- [x] Version bumped and tagged — **N/A.** No release was made; the package is private and unpublished.
- [x] CI green on `main`, verified by SHA.
- [x] Deployment — **not triggered.** Every change tonight is documentation, gate scripts and CI config; the only change reaching `dist/` is 201 bytes of removed dead CSS. Production already serves the current content and passes smoke. Deploying would spend a Hostinger SSH connection to ship nothing a visitor can see.
- [x] This file complete.

### Commits

| SHA                             | What                                                                                   |
| ------------------------------- | -------------------------------------------------------------------------------------- |
| `e6703a6`                       | Gate coverage: every built page, and a JS budget that could actually fail              |
| `c3026c4`                       | Docs truthed up after the Astro 7 upgrade; public-repo gaps closed; dependency hygiene |
| `b0b0ed3`                       | Four documents corrected that described the pre-increment-3 repo; dead CSS removed     |
| _(this file + NEEDS-REVIEW.md)_ | The audit record                                                                       |
