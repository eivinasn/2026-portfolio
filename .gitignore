# Dependencies
node_modules/

# Builds
dist/
.astro/
.out/
.cache/
.vercel/
.netlify/
.turbo/

# Env & logs
.env
.env.*
# …except the template. `.env.*` was swallowing it, so three tracked files
# pointed readers at a file that was not in the repo. It holds comments and
# three commented-out keys — no values, ever.
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Private planning document — references besight.io internals, not for publication.
portfolio-uplift-plan.md
